services:
  minio:
    image: minio/minio
    container_name: minio
    restart: always
    ports:
      - "9000:9000"
      - "9001:9001"
    networks:
      - minionetwork
    volumes:
      - minio_data:/data
    environment:
      MINIO_ROOT_USER: ROOTUSER
      MINIO_ROOT_PASSWORD: TpO8xuG48A
      MINIO_DEFAULT_BUCKETS: defualt_bucket
networks:
  networks:
    driver: bridge    
volumes:
  minio_data:
    driver: local